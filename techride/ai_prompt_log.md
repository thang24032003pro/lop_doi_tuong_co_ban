Prompt 1: Trong lập trình hướng đối tượng, việc thiết lập trạng thái ban đầu cho một Object có vai trò gì quan trọng để đảm bảo tính đồng nhất dữ liệu của hệ thống lớn? Nếu bỏ trống không gán giá trị thì JavaScript sẽ xử lý ra sao?

Prompt 2: Tôi định đưa cả thuộc tính status vào làm tham số của constructor() để người dùng tự nhập khi tạo xe mới. Việc này có làm mất đi tính đồng bộ của hệ thống quản lý trạng thái mặc định không? Phân tích giúp tôi.

Prompt 3: Khi viết một phương thức dùng chung như calculateFare(distance) bên trong class, từ khóa this sẽ hoạt động như thế nào để trỏ đúng vào thuộc tính vehicleType của riêng đối tượng đang gọi hàm đó mà không bị lẫn sang xe khác?
